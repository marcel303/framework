include engine/ShaderPS.txt

shader_in vec2 texcoord;

uniform sampler2D source;
uniform float opacity;

void main()
{	
	vec4 color = texture(source, texcoord);

	// add some translucency
	color.a = mix(0.0, 0.8, color.a);

	color = color * opacity;

    shader_fragColor = color;
}
