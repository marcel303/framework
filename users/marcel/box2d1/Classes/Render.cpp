#include "Render.h"

void DrawRect(SpriteGfx* gfx, float x, float y, float sx, float sy)
{
/*	glBegin(GL_QUADS);
	glVertex3f(x - sx, y - sy, 1.0f);
	glVertex3f(x + sx, y - sy, 1.0f);
	glVertex3f(x + sx, y + sy, 1.0f);
	glVertex3f(x - sx, y + sy, 1.0f);
	glEnd();*/
}
