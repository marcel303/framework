include engine/ShaderPS.txt
include 210-lighting.txt

shader_in vec3 v_position;

void main()
{
	vec3 color = calculateLighting(v_position, true);

	color = pow(color, vec3(1.0/2.2));

	shader_fragColor = vec4(color, 1.0);
}
