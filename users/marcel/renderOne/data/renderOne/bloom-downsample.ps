include engine/ShaderPS.txt

uniform sampler2D source;

shader_in vec2 texcoord;

void main()
{
	// todo : OpenGL version needs an offset (?)

	vec3 color = texture(source, texcoord).rgb;

	shader_fragColor = vec4(color, 1.0);
}