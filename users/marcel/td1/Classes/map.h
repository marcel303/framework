#pragma once

/*
enum TileType
{
	TileType_Opaque,
	TileType_Normal
	TileType_Fast,
	TileType_Slow
};

class Tile
{
public:
	TileType mType;
};

class TileMap
{
public:
	void Load(Stream* stream);
	void Save(Stream* stream);
	
	void Update(float dt);
	void Render();
	
	void Size_set(int sx, int sy);
	
private:
	Tile* mTiles;
	int mSx;
	int mSy;
};
*/
